# ── Virtual environment & caches ──────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# ── Environment & secrets ─────────────────────────────────────────────────────
.env
.env.*

# ── Claude Code local settings (machine-specific permissions) ─────────────────
.claude/settings.local.json

# ── Generated visualizations (legacy, not in data/) ──────────────────────────
backups/
wiki_graph.html
wiki_topology.png
data/vector_topology.png

# ── SQLite journal files ──────────────────────────────────────────────────────
*.db-shm
*.db-wal

# ── Python packaging ─────────────────────────────────────────────────────────
*.egg-info/
dist/
build/

# ── Stale ChromaDB segments (only chroma.sqlite3 matters) ────────────────────
data/chroma/

# ────────────────────────────────────────────────────────────────────────────
# Intentionally TRACKED:
# data/ds_wiki.db        — source of truth
# data/rrp/              — RRP bundles
# data/chroma_db/        — ChromaDB vector index (both machines run bge-large 1024-dim)
# data/wiki_history.db   — embedding history snapshots
# data/viz/              — visualization outputs
# data/viz_outputs/      — viz runner outputs
# data/reports/          — Fisher Suite HTML reports
# Outside Ref/           — external analysis and reference documents
# ────────────────────────────────────────────────────────────────────────────
