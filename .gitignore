# ── Virtual environment & caches ──────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# ── Environment & secrets ─────────────────────────────────────────────────────
.env
.env.*

# ── Claude Code local settings (machine-specific permissions) ─────────────────
.claude/settings.local.json

# ── Generated visualizations (legacy, not in data/) ──────────────────────────
backups/
wiki_graph.html
wiki_topology.png
data/vector_topology.png

# ── SQLite journal files ──────────────────────────────────────────────────────
*.db-shm
*.db-wal

# ── Python packaging ─────────────────────────────────────────────────────────
*.egg-info/
dist/
build/

# ── Stale ChromaDB segments (only chroma.sqlite3 matters) ────────────────────
data/chroma/
# Stale per-collection UUID segment dirs in data/chroma_db/ (only chroma.sqlite3 is the source of truth)
data/chroma_db/*/

# ── macOS / OS artifacts ─────────────────────────────────────────────────────
.DS_Store
**/.DS_Store
Thumbs.db

# ────────────────────────────────────────────────────────────────────────────
# Intentionally TRACKED:
# data/ds_wiki.db        — source of truth
# data/rrp/              — RRP bundles
# data/chroma_db/chroma.sqlite3 — ChromaDB index (both machines run bge-large 1024-dim;
#                                 per-collection UUID subdirs are stale segments — gitignored above)
# data/wiki_history.db   — embedding history snapshots
# data/viz/              — visualization outputs
# data/viz_outputs/      — viz runner outputs
# data/reports/          — Fisher Suite HTML reports
# Outside Ref/           — external analysis and reference documents
# ────────────────────────────────────────────────────────────────────────────

# ── Generated / rebuildable databases (too large for GitHub) ──────────────────
# wiki_history.db is rebuildable from committed migration scripts:
#   - m0_*.py (confidence_calibration records)
#   - create_external_anchors_*.py, create_domain_taxonomy_*.py (schemas)
#   - apply_domain_retag_*.py, insert_info1_anchors_*.py (content)
#   - src/sync.py (embedding snapshots)
data/wiki_history.db
